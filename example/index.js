import doapp from '../src/index';
console.log(doapp());
// 1. Initialize
// const app = doapp();

// 2. Plugins
// app.use({});

// 3. Model
// app.model(require('./models/example'));

// 4. Router
// app.router(require('./router'));

// 5. Start
// app.start('#root');
